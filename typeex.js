function processStock(st) {
    console.log(st.name + ' ' + st.id);
}
processStock({ name: "niketh", id: 100 });
function processValue(st) {
    console.log(st.price + ' ' + st.interest);
}
processValue({ price: 1900, interest: 0.9 });
